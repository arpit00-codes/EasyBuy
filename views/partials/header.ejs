<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EasyBuy</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="font-['helvetica_now_display']">
    <nav class="w-full justify-between px-5 py-3">
      <h3 class="text-xl">Easy Buy</h3>
      <% var loggedin = typeof loggedin != "undefined" ? loggedin : false %>
      <div class="flex  justify-end gap-5">
        <a href="/shop" class="text-black-500">Shop</a>
        <a href="/cart" class="text-black-500">Cart</a>
        <a href="/contact" class="text-black-500">My Account</a>
        <% if (loggedin) { %>
          <a href="/users/logout" class="text-red-600">Logout</a>
        <% } else { %>
          <a href="/"class="text-blue-600">Login</a>
        <% } %>
      </div>
    </nav>
  </body>
</html>
